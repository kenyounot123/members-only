<h1>New Post</h1>
<%= form_with model: @post do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title, placeholder: "Title name here" %>

  <%= form.label :body %>
  <%= form.text_area :body %>

  <%= form.submit "Submit" %>
<% end %>